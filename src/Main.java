import SERVER.ServerClass;

/**
 * This is the main class that handles the execution of th program.
 */
public class Main
{
    /**
     * This method executes the program
     * @param args all the arguments needed to run the program
     */
    public static void main(String[] args)
    {
        /*
            Here I am creating an instance of the Server class. The work to create the server is done by the constructor.
         */
        ServerClass server = new ServerClass();
    }
}
